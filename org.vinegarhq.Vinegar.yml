app-id: org.vinegarhq.Vinegar
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang
command: vinegar

finish-args:
  - --share=network
  - --share=ipc
  # https://github.com/flathub/flathub/issues/6171
  - --socket=x11
  - --socket=wayland
  - --socket=pulseaudio
  # Necessary for controller support (important on Steam Deck)
  - --device=all
  # Necessary for VMProtect to function.
  - --allow=devel
  - --filesystem=xdg-run/app/com.discordapp.Discord:create
  # Convenience for the user to save their Roblox Studio projects.
  - --filesystem=xdg-documents
  - --env=GST_PLUGIN_SYSTEM_PATH=/app/lib/gstreamer-1.0:/usr/lib/x86_64-linux-gnu/gstreamer-1.0
  - --env=PATH=/app/bin:/usr/bin:/usr/lib/extensions/vulkan/gamescope/bin

separate-locales: false
cleanup:
  - /share/man

modules:
  - name: vinegar
    buildsystem: simple
    build-options:
      env:
        GOBIN: /app/bin
        GOROOT: /usr/lib/sdk/golang
    build-commands:
      - make install GO=$GOROOT/bin/go PREFIX=/app
    sources:
      - type: archive
        url: https://github.com/vinegarhq/vinegar/archive/26a0001295d16e55aa8e78d55b69413bba1c89d6.tar.gz
        sha256: d9418174505a6b9ae826276b77a8736a0a59fed58becb5e41915304669b224aa
      - go.mod.yml

  - name: wine
    buildsystem: simple
    build-commands:
      - cp -r * /app/
    sources:
      - type: archive
        url: https://github.com/vinegarhq/wine-builds/releases/download/10.16/vinegarhq-wine-10.16.tar.xz
        sha256: aa8913ba045ad7c25bd07670acc130088b026e39e863ec50bbed8a8cabfe84cf
